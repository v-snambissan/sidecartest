<source>
  @type tail
  path /var/log/app/requests.log
  pos_file /var/log/td-agent/requests.pos
  tag app.*
  format none
</source>

<match app.**>
  @type azure_monitor
  # Configuration for Azure Monitor logging
</match>
